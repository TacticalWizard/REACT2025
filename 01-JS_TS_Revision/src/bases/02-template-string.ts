
const firstName = 'Eze';
const lastName = "O'rsingher";

const fullName = `El nombre es: ${firstName} ${lastName}`; //back ticks allow us to use multine-strings

console.log(firstName + ' ' + lastName);
console.log(fullName);